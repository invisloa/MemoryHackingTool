<Window x:Class="MemoryHackingTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MemoryHackingTool"
        mc:Ignorable="d"
        Title="Memory Scanner" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBox x:Name="ProcessNameTextBox" Width="200" Margin="5" />
            <Button x:Name="ScanButton" Content="Start Scan" Width="100" Margin="5" Click="ScanButton_Click"/>
            <Button x:Name="SubScanButton" Content="Subscan" Width="100" Margin="5" Click="SubScanButton_Click"/>
            <ComboBox x:Name="ValueTypeComboBox" Width="100" Margin="5">
                <ComboBoxItem Content="Byte" IsSelected="True"/>
                <ComboBoxItem Content="Short"/>
                <ComboBoxItem Content="Long"/>
            </ComboBox>
            <TextBox x:Name="ValueTextBox" Width="100" Margin="5" />
        </StackPanel>

        <ListBox x:Name="CriteriaListBox" Grid.Row="1" Margin="10" Height="100">
            <ListBoxItem Content="Increased"/>
            <ListBoxItem Content="Decreased"/>
            <ListBoxItem Content="Same as Before"/>
            <ListBoxItem Content="Same as Original"/>
        </ListBox>

        <DataGrid x:Name="ResultsDataGrid" Grid.Row="2" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*" />
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
